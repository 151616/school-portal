rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Firestore is not part of the active client app path right now.
      // Keep it disabled until explicit document-level rules are needed.
      allow read, write: if false;
    }
  }
}
